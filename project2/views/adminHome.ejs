<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Meta tags for character encoding and responsive design -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Title of the webpage -->
    <title>Admin Home</title>
    <style>
        /* Reset body and HTML margins and padding, and set font style */
        body, html {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
        }

        /* Main container with background image */
        .container {
            background-image: url('/images/beachBackground.jpg'); /* Path to background image */
            background-size: cover; /* Cover the entire page with the image */
            background-position: center; /* Center the image */
            height: 100vh; /* Full viewport height */
            display: flex; /* Use flexbox for layout */
            flex-direction: column; /* Arrange content vertically */
            justify-content: center; /* Center content vertically */
            align-items: center; /* Center content horizontally */
            color: white; /* Set text color to white */
        }

        /* ATM heading */
        .admin-heading {
            font-size: 48px; /* Large font size for "ATM" */
            font-weight: bold; /* Bold text */
            margin-bottom: 20px; /* Space below the heading */
        }

        /* Bank image styling */
        .bank-image {
            width: 120px; /* Set image width */
            height: auto; /* Maintain aspect ratio */
            margin-bottom: 20px; /* Space below the image */
        }

        /* Black box styling for admin interface */
        .admin-box {
            background-color: #000; /* Semi-transparent black background */
            color: white;
            border-radius: 20px; /* Rounded corners */
            box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.5); /* Add shadow for depth */
            width: 500px; /* Fixed width */
            padding: 40px; /* Padding inside the box */
            text-align: center; /* Center align text */
        }

        /* Styling for the main heading in the admin interface */
        .admin-box h2 {
            font-size: 36px; /* Font size for the heading */
            margin-bottom: 30px; /* Space below the heading */
        }

        /* Styling for the welcome message */
        .admin-box p {
            font-size: 32px; /* Font size for the welcome message */
            margin-bottom: 30px; /* Space below the welcome message */
        }

        /* Grouping buttons for admin functionalities */
        .button-group {
          display: grid; /* Grid layout for buttons */
          grid-template-columns: repeat(2, 1fr); /* Two columns */
          gap: 20px; /* Space between buttons */
        }

        /* Individual button styling */
        .button-group button {
            background-color: #222; /* Dark background for buttons */
            color: white; /* White text */
            border: none; /* Remove default border */
            border-radius: 8px; /* Rounded corners */
            padding: 20px; /* Add padding inside buttons */
            font-size: 20px; /* Larger button text */
            cursor: pointer; /* Pointer cursor on hover */
        }

        /* Button hover effect */
        .button-group button:hover {
            background-color: #444; /* Darker blue on hover */
        }
    </style>
</head>
<body>
  <!-- Main container with a background image -->
  <div class="container">
    <!-- ATM heading -->
    <div class="admin-heading">Admin</div>
    <!-- Bank image -->
    <img src="/images/bankImage2.png" alt="Bank Icon" class="bank-image">
        <!-- Black box containing admin interface content -->
        <div class="admin-box">
            <!-- Main heading for the admin home page -->
            <h2>Welcome, <%= workerName || 'Admin' %>!</h2>
            <!-- Welcome message for the admin -->
            <p>Please select the following</p>
            <!-- Button group for admin functionalities -->
            <div class="button-group">
                <!-- Button for viewing ATM status -->
                <button onclick="location.href='/admin/<%= workerID %>/status'">ATM Status</button>
                <!-- Button for viewing ATM transaction history -->
                <button onclick="location.href='/admin/<%= workerID %>/transHistory'">ATM Transactions History</button>
                <!-- Button for viewing ATM service history -->
                <button onclick="goToPage('/serviceHistory')">ATM Service History</button>
                <!-- Button for initiating ATM cash restock -->
                <button onclick="goToPage('/cashRestock')">ATM Cash Restock</button>
                <!-- Button for restarting the admin workflow (redirect to admin start) -->
                <button onclick="logOut()">ATM Restart</button>
                <button onclick="logOut()">Log Out</button>
            </div>
            <script>
              function goToPage(destination) {
                  window.location.href = window.location.pathname.replace('/adminHome', destination);
              }
              function logOut() {
                  window.location.href = '/'
              }
          </script>
        </div>
    </div>
</body>
</html>
