<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login</title>
    <style>
        /* Reset */
        body, html {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
        }

        /* Main container with background image */
        .container {
            background-image: url('/images/beachBackground.jpg'); /* Path to background image */
            background-size: cover; /* Cover the entire page */
            background-position: center; /* Center the image */
            height: 100vh; /* Full viewport height */
            display: flex;
            justify-content: space-between; /* Align sections horizontally */
            align-items: center; /* Center content vertically */
            color: white; /* White text color */
            padding: 0 50px; /* Space on sides */
        }

        /* Left section for ATM and logo */
        .left-section {
            text-align: center; /* Center align text */
        }

        .atm-heading {
            font-size: 48px; /* Large font size for "ATM" */
            font-weight: bold; /* Bold text */
            margin-bottom: 20px; /* Space below the heading */
        }

        .bank-image {
            width: 650px; /* Set image width */
            height: auto; /* Maintain aspect ratio */
            margin-bottom: 20px; /* Space below the image */
        }

        /* Black box styling */
        .login-box {
            background-color: rgba(0, 0, 0, 0.8); /* Semi-transparent black background */
            border-radius: 20px; /* Rounded corners */
            box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.5); /* Add shadow for depth */
            width: 400px; /* Fixed width */
            padding: 30px; /* Internal padding */
            text-align: center; /* Center align text */
        }

        .login-box h2 {
            font-size: 28px; /* Font size for heading */
            margin-bottom: 20px; /* Space below heading */
        }

        .form-group {
            margin-bottom: 20px; /* Space between form fields */
        }

        .form-group label {
            display: block; /* Label on its own line */
            font-size: 18px; /* Font size for labels */
            margin-bottom: 8px; /* Space below label */
        }

        .form-group input {
            width: 100%; /* Full width input fields */
            padding: 10px; /* Internal padding */
            font-size: 16px; /* Input font size */
            border: none; /* Remove borders */
            border-radius: 8px; /* Rounded corners */
            background-color: #f2f2f2; /* Light gray background */
            color: black; /* Black text */
        }

        .button-group {
            display: flex; /* Arrange buttons horizontally */
            justify-content: space-between; /* Space out buttons */
            margin-top: 20px; /* Space above buttons */
        }

        .button-group button {
            background-color: #5a86d6; /* Blue button background */
            color: white; /* White text */
            font-size: 16px; /* Button font size */
            padding: 10px 20px; /* Button padding */
            border: none; /* Remove borders */
            border-radius: 8px; /* Rounded corners */
            cursor: pointer; /* Pointer cursor on hover */
            width: 48%; /* Make buttons equal width */
        }

        .button-group button:hover {
            background-color: #4669ad; /* Darker blue on hover */
        }

        /* Admin Login Button */
        .admin-login {
            position: absolute; /* Position it at the bottom of the page */
            bottom: 20px; /* Space from the bottom */
            left: 50%; /* Center horizontally */
            transform: translateX(-50%); /* Adjust to center */
            background-color: #5a86d6; /* Blue button background */
            color: white; /* White text */
            font-size: 16px; /* Button font size */
            padding: 10px 20px; /* Button padding */
            border: none; /* Remove borders */
            border-radius: 8px; /* Rounded corners */
            cursor: pointer; /* Pointer cursor on hover */
        }

        .admin-login:hover {
            background-color: #4669ad; /* Darker blue on hover */
        }

        .error-message {
            color: red; /* Red for error messages */
            margin-top: 10px; /* Space above the error message */
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Left Section -->
        <div class="left-section">
            <h1 class="atm-heading">Admin</h1>
            <img src="/images/bankImage2.png" alt="Bank Logo" class="bank-image">
        </div>
        
        <!-- Right Section (Login Form) -->
        <div class="login-box">
            <h2>Sign In</h2>
            <form action="/admin/login" method="POST">
                <div class="form-group">
                    <label for="workerUsername">Username</label>
                    <input type="text" id="workerUsername" name="workerUsername" placeholder="Enter your username" required>
                </div>
                <div class="form-group">
                    <label for="workerPassword">Password</label>
                    <input type="password" id="workerPassword" name="workerPassword" placeholder="Enter your password" required>
                </div>
                <div class="button-group">
                    <button type="submit">Sign In</button>
                    <a href="/" class="admin-login">Client Login</a>
                </div>
                <% if (errorMessage) { %>
                <p class="error-message"><%= errorMessage %></p>
                <% } %>
            </form>
        </div>
    </div>
</body>
</html>
