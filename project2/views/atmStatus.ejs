<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Meta tags for character encoding and responsive design -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Title of the webpage -->
    <title>ATM Status</title>
    <style>
        /* Reset body and HTML margins and padding, and set font style */
        body, html {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
        }

        /* Main container with background image */
        .container {
            background-image: url('/images/beachBackground.jpg'); /* Path to background image */
            background-size: cover; /* Cover the entire page with the image */
            background-position: center; /* Center the image */
            height: 100vh; /* Full viewport height */
            display: flex; /* Use flexbox for layout */
            flex-direction: column; /* Arrange content vertically */
            justify-content: center; /* Center content vertically */
            align-items: center; /* Center content horizontally */
            color: white; /* Set text color to white */
        }

        .admin-heading {
            font-size: 48px; /* Larger font size for the Admin heading */
            font-weight: bold; /* Bold text */
            margin-bottom: 20px; /* Space below the heading */
        }

        .bank-image {
            width: 120px; /* Set image width */
            height: auto; /* Maintain aspect ratio */
            margin-bottom: 20px; /* Space below the image */
        }

        /* Black box styling for admin interface */
        .admin-box {
            background-color: #000; /* Semi-transparent black background */
            color: white;
            border-radius: 20px; /* Rounded corners */
            box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.5); /* Add shadow for depth */
            width: 500px; /* Fixed width */
            padding: 40px; /* Padding inside the box */
            text-align: center; /* Center align text */
        }

        /* Styling for the main heading in the admin interface */
        .admin-box h2 {
            font-size: 36px; /* Font size for the heading */
            margin-bottom: 30px; /* Space below the heading */
        }

        /* Styling for the status message */
        .admin-box .status {
            font-size: 24px; /* Font size for the status message */
            margin: 20px 0; /* Space above and below the status message */
        }

        .back-button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #333;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .back-button:hover {
            background-color: #555;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- ATM heading -->
        <div class="admin-heading">Admin</div>
        <!-- Bank image -->
        <img src="/images/bankImage2.png" alt="Bank Icon" class="bank-image">
        <!-- Black box containing ATM status content -->
        <div class="admin-box">
            <h2>ATM Status</h2>
            <p class="status">Status: <%= status %> %</p>
            <button class="back-button" onclick="goBack('/admin/<%= workerID %>/home')">Back To Home</button>
        </div>
    </div>
    <script>
        // Function to navigate back to the home page
        function goBack(destination) {
            window.location.href = destination;
        }
    </script>
</body>
</html>